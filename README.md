# MediaOperator

运营自动化 Python 仓库。

## Structure

- `docs/`: 项目文档与方案记录
- `src/mediaoperator/`: Python 源码
- `tests/`: 测试目录
- `.agents/`: 智能体配置目录
- `.claude`: 指向 `.agents/claude` 的 Claude Code 配置入口

## Development

```bash
python -m pip install -e .
mediaoperator
```
