[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "MediaOperator"
version = "0.1.0"
description = "Media operation automation workspace."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [
  { name = "H2OS Labs" },
]
dependencies = []

[project.scripts]
mediaoperator = "mediaoperator.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/mediaoperator"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
